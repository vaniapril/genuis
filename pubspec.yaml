name: genuis
description: Code generation for theme classes based on files from the assets folder and its structure.
version: 0.0.4
repository: https://github.com/vaniapril/genuis
issue_tracker: https://github.com/vaniapril/genuis/issues

environment:
  sdk: ^3.8.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^3.0.0
  source_gen: ^3.0.0
  yaml: ^3.1.3
  path: ^1.9.1
  xml: ^6.5.0
  dart_style: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

platforms:
  linux:
  windows:
  macos:

topics:
 - theme
 - theme-extension
 - assets
 - codegen
 - build-runner
