name: genuis
version: 0.0.1
homepage: none

environment:
  sdk: ">=3.5.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^2.4.1
  source_gen: ^2.0.0
  yaml: ^3.1.2
  path: ^1.9.0
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

#flutter:
